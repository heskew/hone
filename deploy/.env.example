# Database encryption key (required)
HONE_DB_KEY=your-secure-passphrase

# Optional: Ollama for AI-powered merchant classification and normalization
# Used during CSV import for auto-tagging and extracting clean merchant names
# OLLAMA_HOST=http://localhost:11434
# OLLAMA_MODEL=gemma3

# Optional: AI Orchestrator for Explore Mode and agentic analysis
# Enables conversational queries ("What did I spend last month?") and
# deeper analysis with tool-calling (spending anomaly explanations, etc.)
# Uses Ollama's Anthropic-compatible Messages API
# Recommended: llama3.1 has good tool-calling support
# ANTHROPIC_COMPATIBLE_HOST=http://localhost:11434
# ANTHROPIC_COMPATIBLE_MODEL=llama3.1

# Recommended: Cloudflare Access JWT validation (cryptographic auth verification)
# Get team name from: https://one.dash.cloudflare.com/<account-id>/<team-name>/...
# Get AUD tag from: Access > Applications > your app > Overview
# CF_TEAM_NAME=your-team-name
# CF_AUD_TAG=your-aud-tag-here

# Optional: API keys for internal service authentication (comma-separated)
# Use this for machine-to-machine auth (e.g., Mac training script → Pi server)
# Generate with: openssl rand -hex 32
# HONE_API_KEYS=your-api-key-here

# Optional: Trusted networks that bypass authentication (comma-separated)
# Use for local network access without Cloudflare
# Supports individual IPs or CIDR notation
# HONE_TRUSTED_NETWORKS=192.168.1.0/24,10.0.0.5

# Optional: Trusted proxies whose X-Forwarded-For headers are trusted (comma-separated)
# Required when running behind a reverse proxy (Traefik, nginx, etc.) with trusted networks
# Without this, Hone sees the proxy's IP instead of the real client IP
# Common values: 10.42.0.0/16 (k3s), 10.244.0.0/16 (k8s), 172.17.0.0/16 (Docker)
# HONE_TRUSTED_PROXIES=10.42.0.0/16
